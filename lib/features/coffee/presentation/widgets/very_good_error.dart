import 'package:flutter/material.dart';

class VeryGoodError extends StatelessWidget {
  const VeryGoodError({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/images/error.jpg',
    );
  }
}
